# Variables of the production and staging deployment (PRD-18 RF-3, RF-8).
#
# This file is the inventory, not the values: every variable named, none filled in. The
# values live in Doppler and reach Dokploy through its Doppler secrets provider (D-33), so
# there is no .env on the server and no secret in this repository (hard rule 6).
#
# ---------------------------------------------------------------------------
# How this is wired
# ---------------------------------------------------------------------------
#
#   Doppler project `dizen-backend`
#     ├── config `stg`  ->  Dokploy provider `doppler-stg`  ->  project dizen-v2-staging
#     └── config `prd`  ->  Dokploy provider `doppler-prd`  ->  project dizen-v2-production
#
# One Doppler config per environment and one Dokploy provider per config, which is what keeps
# RF-2 true: nothing shared between staging and production, including the token that reads
# the secrets.
#
# 1. In Doppler, open the config and generate a **Service Token** in the Access tab. It is
#    read-only and scoped to that one project and config, so a leaked token exposes that
#    config and nothing else. Personal and CLI tokens also work and are worse.
# 2. In Dokploy, Settings -> Secrets -> Add Provider -> Doppler. Name it `doppler-stg` or
#    `doppler-prd`, paste the token, test the connection, and assign it to the environment.
# 3. In the environment variables of the Dokploy project, reference each secret:
#
#       IDENTITY_DATABASE_URL=${{vault.doppler-stg.IDENTITY_DATABASE_URL}}
#
#    The non-secret variables below are written literally; only the ones marked SECRET need
#    to go through the vault.
#
# Locally there is no .env either: `make up` runs the compose under `doppler run` when the
# CLI is configured, and falls back to the committed defaults when it is not.

# --- Environment ------------------------------------------------------------
# staging | production. It names the compose project and prefixes every Traefik router, so
# the two environments -- and the v1 stack that shares this Traefik -- cannot collide.
# Required.
DIZEN_ENV=staging

# trace | debug | info | warn | error. `info` in production, `debug` in staging.
LOG_LEVEL=info

# --- Images (D-35) ----------------------------------------------------------
# The server does not compile. Images are built and published by
# .github/workflows/images.yml on every push to `staging` and `main` and on every `v*` tag,
# and deploying is choosing which one to run.
#
# There is no VERSION, COMMIT or BUILD_DATE here any more: the workflow that builds the image
# bakes them in, because it is the only place that knows them.
#
# These two go in Dokploy directly, NOT in Doppler, and not only because they are not
# secrets. They are configuration of the deployment rather than of the application: the
# services never read them, Compose consumes them at deploy time to decide which artifact to
# start, and by the time a Go process exists they have done their job. Doppler holds what the
# application needs in order to run.
#
# The practical half of the argument is the rollback. Going back is changing IMAGE_TAG to a
# `sha-` tag and redeploying, and that belongs on the same screen as the deploy button --
# not behind a hop to a secrets manager, waiting for a sync, in the middle of an incident.

# Where the images live. Required.
#   ghcr.io/nicodanke/dizen-v2-backend
IMAGE_REGISTRY=

# Which image to run. Required.
#
#   staging      IMAGE_TAG=staging      moves with every push to the staging branch
#   production   IMAGE_TAG=v1.2.3       pinned to a release, never moving
#
# Every image is also published as `sha-<short>`, which makes a rollback a matter of changing
# this variable and redeploying, with no rebuild of an old commit.
IMAGE_TAG=

# --- Domains (RF-5) ---------------------------------------------------------
# v2 runs alongside the v1 stack, which already owns api.dizen.pro and grpc-*.dizen.pro, so
# v2 lives under its own subdomain until v1 is retired (D-31):
#
#   staging      api.staging.v2.dizen.pro   grpc.staging.v2.dizen.pro   admin-api.staging.v2.dizen.pro
#   production   api.v2.dizen.pro           grpc.v2.dizen.pro           admin-api.v2.dizen.pro
#
# The cutover, when it comes, is a DNS and host-rule change and nothing else.

# Public REST gateway, path-routed to identity, tours and booking. Required.
API_HOST=

# gRPC for the mobile app, HTTP/2 end to end (RF-6). Required.
GRPC_HOST=

# API of the dashboard, the `admin` service. Required.
# The dashboard itself is static and deploys from dizen-v2-web.
ADMIN_API_HOST=

# Certificate resolver configured in the Traefik of Dokploy. The v1 stack uses `letsencrypt`.
CERT_RESOLVER=letsencrypt

# --- Edge protection --------------------------------------------------------
# RF-5: staging carries an extra basic authentication over the REST hosts. Set it to
# ",<env>-v2-basicauth@docker" in staging -- the leading comma is part of the value, since it
# is appended to the middleware chain -- and leave it EMPTY in production.
#
#   staging:      EXTRA_MIDDLEWARES=,staging-v2-basicauth@docker
#   production:   EXTRA_MIDDLEWARES=
#
# The gRPC hosts are deliberately left out: a gRPC client sends its own Authorization header,
# and basic authentication over it would break the app in staging without adding anything,
# because those calls already need a valid JWT.
EXTRA_MIDDLEWARES=

# htpasswd lines for that middleware, comma separated. Generate with:
#   htpasswd -nbB dizen '<password>'
#
# CAREFUL: a bcrypt hash is full of `$`, and `$` is what Compose interpolates. Every `$` has
# to be doubled, or the hash arrives mutilated and the login simply never works:
#
#   htpasswd output   dizen:$2y$05$P4s...
#   what goes here    dizen:$$2y$$05$$P4s...
#
# SECRET. Only needed in staging.
BASIC_AUTH_USERS=

# --- Data stores (D-32) -----------------------------------------------------
# Postgres, Redis and RabbitMQ are managed outside this compose, the same way the v1 stack
# does it, and arrive as connection strings. One database and one credential per service, no
# cross-access (hard rule 2): a leaked DSN exposes one domain.
#
# All five SECRET and required. Format:
#   postgres://<user>:<password>@<host>:5432/<database>?sslmode=require
#
# ON NEON, USE THE DIRECT ENDPOINT, NOT THE POOLED ONE. The pooled host -- the one with
# `-pooler` in it -- is PgBouncer in transaction mode, and two things this repository depends
# on break against it:
#
#   - golang-migrate takes a SESSION-level advisory lock, which is the whole mechanism RF-7
#     relies on to stop two replicas migrating at once. In transaction pooling the lock is
#     taken on one backend and released on another, so it protects nothing -- and it fails
#     the way concurrency bugs fail, which is rarely and at the worst moment.
#   - pgx uses the extended protocol with prepared statements by default (pkg/database opens
#     the pool with plain ParseConfig), and transaction pooling does not support them.
#
# The pooled endpoint becomes worth revisiting when connection count is a real problem; it
# would need `default_query_exec_mode=simple_protocol` and migrations run separately.
#
# tours will need PostGIS once the schema of 02 lands (01 section 5); today the migrations
# only create the outbox table, so nothing needs the extension yet. When it does, the
# migration has to `create extension if not exists postgis`, which Neon supports.
IDENTITY_DATABASE_URL=
TOURS_DATABASE_URL=
BOOKING_DATABASE_URL=
ADMIN_DATABASE_URL=
MAIL_DATABASE_URL=

# The two stores are managed outside this compose (D-32) and their URLs are ASSEMBLED by it
# from the values below, not stored whole. That is deliberate: a full DSN meant the password
# lived here and again in the store's own Dokploy project, and the two had to match by hand.
# They did not, and the symptom was `403 username or password not allowed` -- which reads
# like a wrong password rather than like two values drifting apart.
#
# So set the SAME password you set in the Redis and RabbitMQ projects, and nothing else. The
# host is assembled from DIZEN_ENV, so it cannot drift from the container name either.

# One Redis per environment, shared by the five services: REDIS_NAMESPACE already keeps their
# keys apart, so five instances would be five things to operate for nothing.
#
# SET `maxmemory-policy` TO `noeviction` on that instance, not to an LRU policy. Redis holds
# three kinds of data here and they do not tolerate eviction equally: losing a cached tour is
# free, losing a rate-limit counter costs somebody a free request, and losing an entry of the
# session revocation list makes a revoked token valid again (01 section 6). An LRU policy
# makes that last one a silent security failure, under pressure, that no test would catch.
#
# SECRET, required. Generate with `openssl rand -hex 32`, NOT with `-base64`: this ends up
# inside a URL, and base64 produces `+`, `/` and `=`. A `/` or an `@` in the credentials
# section truncates the parse and the client sends credentials that are not the ones you set.
REDIS_PASSWORD=

# One RabbitMQ per environment. `deploy/rabbitmq/` is a compose stack for it, deployed as its
# own Dokploy project. There is nothing to create inside the broker: the exchange, the queues,
# the dead letter queues and the bindings are declared by the services at startup.
RABBITMQ_USER=dizen

# SECRET, required. Same rule as above: hex, not base64.
RABBITMQ_PASSWORD=

# Escape hatches. Leave them EMPTY unless a store moves to a managed provider with its own
# hostname or TLS; setting either one overrides the assembly above.
REDIS_URL=
AMQP_URL=

# Topic exchange every domain event is published to.
AMQP_EXCHANGE=dizen.events

# --- JWT (EdDSA / Ed25519) --------------------------------------------------
JWT_ISSUER=dizen
JWT_AUDIENCE=dizen-app

# SECRET. Only identity receives it: it is the only service that issues tokens, and the only
# one that can. One pair per environment, and the development one never reaches any of them.
#
#   make jwt-key-pem
#
# Either target works: the loader accepts a PEM with real line breaks and one whose newlines
# arrived escaped as `\n`, because those are the two transports a key travels through and
# both are legitimate. `make jwt-key-pem` is still the one to use here, since a single-line
# value survives every hop between Doppler, Dokploy and the container without depending on
# quoting.
#
# What is no longer possible is the silent half of the old failure: a public key that yields
# no PEM block is now rejected at startup instead of leaving a service healthy and rejecting
# every token it is given.
JWT_PRIVATE_KEY_PEM=

# Public verification keys, PKIX PEM, concatenated. Every service gets these. During a
# rotation the retired key stays here until the last token it signed has expired.
JWT_PUBLIC_KEYS_PEM=

# --- API contract -----------------------------------------------------------
# Oldest client contract version accepted; below it the interceptor answers CLIENT_TOO_OLD
# and the app asks the user to update (PRD-00 RF-2c). Empty disables the check.
MIN_CLIENT_API_VERSION=

# --- Observability ----------------------------------------------------------
# OTLP collector. Empty until RF-14 deploys it, which disables exporting rather than
# retrying against an address that answers nothing.
OTLP_ENDPOINT=

# Fraction of traces sampled. 1.0 in staging, a fraction in production.
TRACE_SAMPLE_RATIO=0.1

# --- Backups (RF-9) -- OFF BY DEFAULT ---------------------------------------
# The backup container is behind the `backup` compose profile, so it does not start unless
# the profile is enabled, and NONE of the variables below are required until it is. Leave
# them out of the Dokploy project entirely rather than pointing them at secrets that do not
# exist: Dokploy resolves a ${{vault....}} reference before it builds anything, and a missing
# secret fails the deployment with "not found in this config".
#
# Turning it on later is: create the bucket, add these secrets, add the variables, and deploy
# with the profile. Nothing else changes -- the container only ever needed the DSNs above and
# a bucket.
#
# Until then the data's only protection is whatever the managed Postgres gives you. Neon
# keeps a point-in-time history, which covers a dropped table but not a deleted project, and
# it is not a restore drill (RNF-3, acceptance criterion 6).

# Destination bucket, and the retention that RF-9 asks for: 7 daily, 4 weekly, 3 monthly.
# Use a different bucket per environment: a restore drill must not be able to touch
# production's copies.
BACKUP_BUCKET=
BACKUP_SCHEDULE=0 3 * * *
BACKUP_TZ=UTC
BACKUP_RETAIN_DAILY=7
BACKUP_RETAIN_WEEKLY=4
BACKUP_RETAIN_MONTHLY=3

# Which rclone backend the bucket speaks. Note that `s3` is the PROTOCOL and not Amazon:
# Cloudflare R2, MinIO and Backblaze all use it, and so does Google Cloud Storage in its
# interoperability mode. Google's native backend is the other option.
#
#   BACKUP_REMOTE_TYPE=s3                        -> fill in the BACKUP_S3_* block
#   BACKUP_REMOTE_TYPE=google cloud storage      -> fill in the BACKUP_GCS_* block
BACKUP_REMOTE_TYPE=s3

# --- if BACKUP_REMOTE_TYPE=s3 ---
# R2 endpoint: https://<account-id>.r2.cloudflarestorage.com
# GCS in interoperability mode: https://storage.googleapis.com, with HMAC keys.
BACKUP_S3_PROVIDER=Cloudflare
BACKUP_S3_ENDPOINT=
BACKUP_S3_REGION=auto

# SECRET. Scope the credential to this bucket alone: it is the one that, if it leaked, would
# hand over a copy of every database.
BACKUP_S3_ACCESS_KEY_ID=
BACKUP_S3_SECRET_ACCESS_KEY=

# --- if BACKUP_REMOTE_TYPE=google cloud storage ---
# The service account needs Storage Object Admin on the bucket and nothing else.
BACKUP_GCS_PROJECT=
BACKUP_GCS_LOCATION=

# SECRET. The whole service account JSON, in one variable.
BACKUP_GCS_SERVICE_ACCOUNT_JSON=

# --- Authoring: routing and tiles (RF-3b, doc 07) ---------------------------
# Both live in staging only: the dashboard is their only user, and a re-import must not
# compete for CPU with user traffic -- or with the v1 stack on the same VPS.

# Regional OSM extract Valhalla builds its tiles from. Geofabrik publishes them by region:
#   https://download.geofabrik.de/south-america/argentina-latest.osm.pbf
VALHALLA_TILE_URL=

# What `tours` uses to reach it. Internal address, never public. Empty in production.
#   staging:      VALHALLA_URL=http://valhalla:8002
#   production:   VALHALLA_URL=
VALHALLA_URL=

# Area planetiler generates the .pmtiles for, in its own naming (07 section 4.2):
#   PLANETILER_AREA=argentina
PLANETILER_AREA=
